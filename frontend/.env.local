# Local environment variables for development
# This file should NOT be committed to git

# Backend API URL for local development (should point to local Flask server)
VITE_API_URL=http://127.0.0.1:5000
